plugins {
    id 'java'
}

group = 'org.oryxel.novibrantvisuals'
version = '1.0'

repositories {
    mavenCentral()
    maven {
        url = "https://repo.opencollab.dev/main/"
    }
}

dependencies {
    compileOnly("org.geysermc.geyser:api:2.8.0-SNAPSHOT")
}